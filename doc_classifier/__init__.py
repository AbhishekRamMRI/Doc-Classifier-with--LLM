"""Core business logic for classifying business documents from PDFs using an LLM.

This package has zero CLI/web dependencies so it can be reused by any front-end
(CLI, Streamlit, FastAPI, etc.) by simply calling `process_file` / `process_files`.
"""
